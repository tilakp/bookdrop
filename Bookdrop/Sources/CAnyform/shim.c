// Intentionally empty. This target exists only to expose anyform.h
// (include/module.modulemap) to Swift via `import CAnyform` — the actual
// implementation is the prebuilt Rust static lib linked into the
// `Bookdrop` executable target's linker settings (see Package.swift).
